spring:
  cloud:
    config:
      uri: http://config-server:8888
  application:
    name: api-gateway
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
# ---
# spring:
#   config:
#     activate:
#       on-profile: docker
#   cloud:
#     config:
#       uri: http://config-server:8888
