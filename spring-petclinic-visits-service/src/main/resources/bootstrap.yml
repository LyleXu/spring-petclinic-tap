spring:
  cloud:
    config:
      uri: http://config-server
  application:
    name: visits-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server/eureka
# ---
# spring:
#   config:
#     activate:
#       on-profile: docker
#   cloud:
#     config:
#       uri: http://config-server:8888
